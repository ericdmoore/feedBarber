import { assertEquals } from 'https://deno.land/std@0.123.0/testing/asserts.ts';

Deno.test('basic1', async () => {
	assertEquals({}, {});
});

Deno.test('basic2', async () => {
	assertEquals({}, {});
});
