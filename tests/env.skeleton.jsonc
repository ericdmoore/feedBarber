/*

Fill out your own values and then copy paste this fill
into an `.env.ts` file and make the object the default export

*/

{
    // needs privleges to read/write the bucket listed below
    // needs privleges to read/write the optional dynamotbale
    "AWS_KEY": "ThekeyToYourAWSAcount",
    "AWS_SECRET": "SomeAwesomeSecret" ,
    "REGION": "us-west-2",
    // "dynamoTable": "sometable",
    "pollybucket": "myFavoriteBucket" ,
    "pollyPrefix": "__deleteMe-"
}